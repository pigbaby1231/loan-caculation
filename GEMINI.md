# Gemini 專案：貸款試算

## 專案概述

這是一個網頁版的貸款試算工具。根據專案位於 `D:\html` 目錄下，推斷此專案是使用 HTML、CSS 和 JavaScript 編寫的。這個專案的目的是提供一個簡單的介面，讓使用者可以計算貸款的月付金。

## 主要檔案

*   `index.html`: 網站的主要 HTML 檔案，包含網頁的結構。
*   `style.css`: 網站的 CSS 樣式檔案。
*   `script.js`: 包含貸款計算邏輯的 JavaScript 檔案。

## 開發

在網頁瀏覽器中開啟 `index.html` 檔案即可執行此專案。